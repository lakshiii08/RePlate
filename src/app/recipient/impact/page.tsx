import React from 'react';
import RecipientImpactView from '@/components/recipient/RecipientImpactView';

export const metadata = {
  title: 'Community Impact — RePlate',
  description: 'Food received, meals served, and environmental waste reduced.',
};

export default function RecipientImpactPage() {
  return <RecipientImpactView />;
}
