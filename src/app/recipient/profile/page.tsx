import React from 'react';
import RecipientProfileView from '@/components/recipient/RecipientProfileView';

export const metadata = {
  title: 'Recipient Profile — RePlate',
  description: 'NGO, shelter, and community center details.',
};

export default function RecipientProfilePage() {
  return <RecipientProfileView />;
}
